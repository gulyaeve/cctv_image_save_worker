services:
  cctv_scr_worker:
    image: cctv_scr_worker
    build:
      context: .
    volumes:
      - /home/user/screenshots:/screenshots
    container_name: cctv_scr_worker
    env_file:
      - .env
    # depends_on:
    #   - db
    #   - rabbitmq
      # - redis
    command: ["python", "main.py"]
    # ports:
    #   - 8000:8000
    networks:
      default:
        aliases:
          - "cctv_scr_worker"
       

networks:
  default:
    name: cctv_network
    external: true